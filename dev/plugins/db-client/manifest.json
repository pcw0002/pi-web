{
	"id": "db-client",
	"name": "Database",
	"version": "0.2.0",
	"description": "Database connection manager and browser: MySQL / PostgreSQL / SQLite / SQL Server / MongoDB / Redis. Schema tree, table structure, paginated data, SQL query editor. Drivers auto-install on first activate.",
	"permissions": [
		"net",
		"tools"
	]
}
