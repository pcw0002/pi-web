{
	"name": "db-client-plugin",
	"private": true,
	"type": "module",
	"description": "pi-web-ui database connection plugin (build-only package.json, not published; runtime drivers are auto-installed by the plugin server)",
	"scripts": {
		"build": "node build.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.25.5"
	}
}
