# Future work

Parked ideas. Not wired into the app, typecheck, or tests.

**Now:** Local Review is the pi-web **Review** tab. Submit writes `.local-review/`; apply is the skill plus `local_review_*` tools. That is the product until it is stable.

**Later, if the team wants it:** a VS Code / Cursor extension so Claude-in-terminal and Cursor users can comment without running this app. Sketch and host contracts: [`packages/README.md`](packages/README.md).

Do not extract `core` / `ui` / MCP until there is a clear want. The apply skill already works in Cursor and Claude Code without that split.
